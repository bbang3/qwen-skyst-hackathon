# Mock Phishing Server

This is a mock phishing FastAPI server designed to demonstrate prompt injection attack. The server provides web pages that appear normal but contain hidden prompt injection attempts that could be extracted when an AI agent scrapes or processes the content.
